// MODYL 8 (DOM)

// let elem = document.querySelector('[data-widget-name]');
// alert(elem.getAttribute('data-widget-name'));
// alert(elem.dataset.widgetName);

// Вибираємо елемент з атрибутом data-widget-name
// const element = document.querySelector('[data-widget-name]');

// // Перевіряємо, чи елемент був знайдений
// if (element) {
//     // Читаємо значення атрибута data-widget-name
//     const widgetName = element.getAttribute('data-widget-name');

//     // Виводимо значення атрибута на консоль
//     console.log('Значення атрибута data-widget-name:', widgetName);
// } else {
//     console.log('Елемент з атрибутом data-widget-name не знайдено.');
// }